# Copy to .env and fill in your values
# NOTE: Game tokens are single-use per session — get one by clicking Play on app.ainm.no
# Pass the token as a CLI arg: python -m grocery_bot.bot <token>
ANTHROPIC_API_KEY=your_anthropic_api_key_here
